import 'package:flutter/cupertino.dart';

class tab_home extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child:Center(
        child: Center(
          child: Text('tab home'),
        ),
      )
      
    );
  }
}