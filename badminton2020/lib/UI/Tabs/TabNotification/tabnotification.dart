import 'package:flutter/material.dart';

class tab_notification extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child:Center(child: Text('notification'),
      )
      
    );
  }
}