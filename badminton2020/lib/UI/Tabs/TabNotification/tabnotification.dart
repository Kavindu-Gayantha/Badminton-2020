import 'package:flutter/material.dart';

class TabNotification extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child:Center(child: Text('notification heheeh',textDirection: TextDirection.ltr),
      )
      
    );
  }
}